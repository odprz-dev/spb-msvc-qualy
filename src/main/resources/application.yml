server:
  port: 8083

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG

spring:
  data:
    mongodb:
      uri: mongodb+srv://root:toor@awsm0free.9ags0kb.mongodb.net/?retryWrites=true&w=majority
      database: microservicios
  application:
    name: CALIFICACION-SERVICE
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
